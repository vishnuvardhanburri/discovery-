feat: XAVIRA Deep Intelligence operator (deep research, show all, batch) — strict ICP gate

Additive deep layer on the existing XAVIRA pipeline (no rewrite of
IntelligenceEngine / LivePublicObservationProvider / evidence / owner / email):
- DeepTypes: DeepProspect/DeepSignal/DeepOwner/DeepContact/qualification/email types
- DeepSignalExtractor: structured signals with strict FACT/OBSERVATION/INFERENCE separation + provenance
- ContactabilityFinder: public-professional contacts only — never guesses emails
- IcpQualificationEngine: 8-dimension strict gate -> PASS/NO_GO/RESEARCH_MORE
- DeepEmailGenerator: only when GO+HIGH owner+usable contact; evidence lineage + uncertainty
- DeepProspectBuilder: orchestrator + dossier writer (artifacts/intelligence/deep/<iso>-<host>.json)
- XaviraOperator: `deep research|file|prospects|help` REPL cmds + `show all` dossier view (adds, no alterations)
- tests/deepOperator.test.ts: 33 tests/105 assertions, fully offline (fakeFetcher+MockProvider)
- tsconfig.xavira.json + package.json: include test files and test chain

Verified: tsc -p tsconfig.xavira.json (0 errors); npm test -> 27+72+105 all pass;
real `deep research https://www.cockroachlabs.com` -> NO_GO, finding NONE,
email NOT generated, dossier persisted (no fabrication).
